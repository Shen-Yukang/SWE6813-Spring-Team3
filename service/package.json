{
  "name": "behavior-aware-matching-service",
  "version": "0.2.0",
  "private": true,
  "description": "Behavior-aware matchmaking service (Express + MongoDB)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "mongoose": "^8.19.2"
  },
  "devDependencies": {
    "jest": "^30.0.5",
    "jest-cucumber": "^4.5.0",
    "mongodb-memory-server": "^10.3.0",
    "supertest": "^7.1.1"
  }
}
